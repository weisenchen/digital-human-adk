google-adk>=1.0.0,<2.0.0
python-dotenv>=1.0.0

# TTS (pick one): edge-tts is free, no API key
edge-tts>=6.0.0
# google-cloud-texttospeech>=2.21.0  # google — needs GCP credentials
# openai>=1.50.0                     # openai — needs OPENAI_API_KEY

# Multi-model support (DeepSeek, Claude via OpenRouter)
openai>=1.50.0
